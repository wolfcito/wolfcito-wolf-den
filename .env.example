# DenLabs Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local or actual secrets to the repository

# ============================================
# SUPABASE
# ============================================
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================
# REOWN APPKIT (Wallet Connection)
# ============================================
# Get your project ID from: https://cloud.reown.com
NEXT_PUBLIC_REOWN_PROJECT_ID=your_project_id_here

# Optional: Custom RPC endpoint (has fallback if not provided)
NEXT_PUBLIC_MAINNET_RPC=https://...

# ============================================
# SELF.XYZ (Identity Verification)
# ============================================
# Configure at: https://self.xyz
NEXT_PUBLIC_SELF_APP_NAME=Wolf Den
NEXT_PUBLIC_SELF_ENDPOINT=https://yourapp.com/api/self/verify
NEXT_PUBLIC_SELF_SCOPE=your-scope-here
NEXT_PUBLIC_SELF_DEEPLINK_CALLBACK=https://yourapp.com/auth

# Optional: Development mode and sandbox
NEXT_PUBLIC_SELF_DEV_MODE=false
SELF_USE_SANDBOX=false

# ============================================
# GOODDOLLAR REWARDS
# ============================================
# GoodDollar Engagement Rewards contract addresses
NEXT_PUBLIC_GD_REWARDS_CONTRACT=0x...
NEXT_PUBLIC_GD_APP_ADDRESS=0x...

# ============================================
# SPRAY DISPERSER
# ============================================
# Spray bulk payout contract address (Celo mainnet)
NEXT_PUBLIC_SPRAY_ADDRESS=0x...

# ============================================
# TABERNA (Live Events)
# ============================================
# Optional: URL for Taberna iframe integration
NEXT_PUBLIC_TABERNA_URL=https://...

# ============================================
# SITE CONFIGURATION
# ============================================
# Your deployed site URL (used for callbacks and metadata)
NEXT_PUBLIC_SITE_URL=https://denlabs.vercel.app

# ============================================
# NOTES
# ============================================
# - All NEXT_PUBLIC_* variables are exposed to the browser
# - SUPABASE_SERVICE_ROLE_KEY is server-side only
# - SELF_USE_SANDBOX is server-side only
# - Restart dev server after changing environment variables
# - For local development with Self.xyz, use a tunnel (ngrok, Cloudflare)
#   to expose your callback endpoint for mobile app verification

# ============================================
# X402 PREMIUM PAYMENT LAYER
# ============================================
# UVDAO facilitator for payment verification (stateless)
X402_FACILITATOR_URL=https://facilitator.ultravioletadao.xyz
X402_RECIPIENT=0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb
X402_TOKEN=usdc

# Development bypass (skip payment verification in dev)
X402_DEV_BYPASS=true

# Health check before returning 402 (prevent payment when facilitator is down)
# If facilitator is unhealthy, return 503 instead of 402
X402_ENABLE_HEALTHCHECK=true  # Default: true
X402_HEALTHCHECK_TIMEOUT=2000  # Timeout in ms (default: 2000)

# Enable /api/x402/conformance in production (default: disabled)
X402_DEV_DIAGNOSTICS=false